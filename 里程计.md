# FishBot ROS 2 通信笔记

  

## 一、三个核心概念

  

**Node（节点）** = ROS 2 网络中的一个参与者。本项目一个节点：`fish_bot_motion_control`。创建节点用 `rclc_node_init_default`。

  

**Topic（话题）** = 数据的管道名。发布者往里写，订阅者从中读，双方互不知道对方是谁。

  

**Message（消息）** = 管道里传的数据格式。如 `/cmd_vel` 用 `Twist`（Vector3 linear + angular），`/odom` 用 `Odometry`（pose + twist + header）。

  

本项目的通信关系：

  

```

PC (teleop) ESP32 节点 PC (rviz)

│ │ │

│── /cmd_vel (Twist) ──→ twist_callback() │

│ │ │

│ timer_callback() ── /odom (Odometry) ──→│

```

  

## 二、微 ROS API 速查

  

按初始化顺序排列：

  

| 函数 | 做什么 |

|------|--------|

| `set_microros_wifi_transports(ssid, pass, ip, port)` | 连 WiFi → 注册 UDP 传输层给 micro-ROS |

| `rcl_get_default_allocator()` | 获取默认内存分配器，后面初始化都要用 |

| `rclc_support_init()` | 初始化 micro-ROS 支持层，和 Agent 握手 |

| `rclc_node_init_default()` | 创建节点，指定节点名 |

| `rclc_executor_init()` | 创建执行器，传入 handle 数量（sub + timer = 2） |

| `rclc_subscription_init_default()` | 创建订阅者：指定消息类型和话题名 |

| `rclc_executor_add_subscription()` | 把订阅者注册到执行器，绑定回调函数 |

| `rclc_publisher_init_best_effort()` | 创建发布者：指定消息类型和话题名（best_effort 意为不保证送达） |

| `rclc_timer_init_default()` | 创建定时器：指定周期（纳秒）和回调函数 |

| `rclc_executor_add_timer()` | 把定时器注册到执行器 |

| `rmw_uros_epoch_synchronized()` | 检查是否已和 Agent 完成时间同步 |

| `rmw_uros_sync_session()` | 向 Agent 请求一次时间同步 |

| `rclc_executor_spin()` | 启动事件循环（阻塞，永不返回） |

| `rcl_publish()` | 发布一条消息到话题 |

  

## 三、两条回调链路

  

### 入站：接收到 `/cmd_vel`

  

```

Executor 检测到新消息

→ twist_callback(msgin)

1. 从 msgin 取出 linear.x（m/s）×1000 → target_linear_speed（mm/s）

2. 从 msgin 取出 angular.z → target_angular_speed（rad/s）

3. kinematics.kinematics_inverse() → 逆解出左右轮目标速度

4. pid_controller[0/1].update_target() → 更新 PID 目标值

```

  

回调时机：`ON_NEW_DATA`——只有新消息到达才触发，不重复处理旧数据。

  

### 出站：发布 `/odom`

  

```

Executor 检测到定时器到期（每 50ms）

→ timer_callback(timer, last_call_time)

1. kinematics.get_odom() → 获取里程计结构体

2. rmw_uros_epoch_millis() → 获取同步后的时间戳

3. 填充 msg_odom 的 header、pose、twist 字段

4. rcl_publish() → 发出

```

  

## 四、线程分布

  

```

┌─ loop() [主线程] ──────────────────────

│ delay(10)

│ kinematics.update_motor_speed() ← 读编码器、更新轮速、更新里程计

│ pid_controller[0/1].update() ← PID 计算

│ motor.updateMotorSpeed() ← 输出 PWM

│

│ 周期：10ms，负责实时控制

└──────────────────────────────────────

  

┌─ micro_ros_task [FreeRTOS 线程] ─────

│ rclc_executor_spin(&executor) ← 阻塞事件循环

│ ├─ 有新 cmd_vel → twist_callback()

│ └─ 定时器到期 → timer_callback() → rcl_publish()

│

│ 周期：事件驱动 + 50ms 定时器

└──────────────────────────────────────

```

  

两线程通过共享变量 `kinematics` 和 `pid_controller` 交换数据，无需加锁（单写单读）。

  

## 五、一个消息的完整旅程

  

以遥控器发一个前进指令为例：

  

```

1. PC 端: ros2 topic pub /cmd_vel ... "{linear:{x:0.1}}"

↓ UDP 广播

2. Agent (micro_ros_agent): 接收 UDP → 转发给连接的 micro-ROS 客户端

↓ WiFi UDP

3. ESP32 micro_ros_task: Executor 收到消息 → Twist 反序列化

↓ 调用 twist_callback()

4. 逆解: V=100mm/s, ω=0 → 左右轮目标 = 100mm/s

↓ 更新 pid_controller.target

5. loop(): 每 10ms 用编码器反馈做 PID → 输出 PWM 驱动电机

↓ 同时 kinematics.update_motor_speed() 更新里程计

6. micro_ros_task: 每 50ms timer_callback 把里程计结构体打包

↓ rcl_publish()

7. PC 端: ros2 topic echo /odom → 看到位置在变化

```

  

## 六、概念速查表

  

| 概念 | 本项目中的值 | micro-ROS API |

|------|------------|--------------|

| 节点名 | `fish_bot_motion_control` | `rclc_node_init_default` |

| 订阅话题 | `/cmd_vel` | `rclc_subscription_init_default` |

| 发布话题 | `/odom` | `rclc_publisher_init_best_effort` |

| 订阅消息类型 | `geometry_msgs/msg/Twist` | 类型宏 `ROSIDL_GET_MSG_TYPE_SUPPORT` |

| 发布消息类型 | `nav_msgs/msg/Odometry` | 类型宏 `ROSIDL_GET_MSG_TYPE_SUPPORT` |

| 订阅回调 | `twist_callback` | `rclc_executor_add_subscription` |

| 定时回调 | `timer_callback` | `rclc_executor_add_timer` |

| 定时周期 | 50ms (20Hz) | `RCL_MS_TO_NS(50)` |

| 发布动作 | `rcl_publish` | 在 `timer_callback` 内调用 |

| 事件循环 | `rclc_executor_spin` | 阻塞运行于独立 FreeRTOS 任务 |

| 帧关系 | `odom` → `base_footprint` | `msg_odom.header.frame_id` |