---
tags:
  - 嵌入式
  - 电赛
created: 2026-06-30
---

# 电赛材料清单

---

## 一、硬件物料

### 紧固件

| 类型 | 规格 | 备注 |
|------|------|------|
| 螺丝 | M3、M2.5 | |
| 螺母 | M3、M2.5 | |
| 尼龙柱 | M3 | |

### 传感器与驱动

| 模块     | 型号 / 方案       | 备注                                                                |
| ------ | ------------- | ----------------------------------------------------------------- |
| 陀螺仪    | 维特陀螺仪         | 放弃 MPU6050，性能太差                                                   |
| 电机驱动   | TB6612        | 最熟悉                                                               |
| 红外循迹   | 不带 MCU 的循迹模块  | 优先考虑                                                              |
| 摄像头    | K230（优先）      | OpenMV 性能较差，备选                                                    |
| 电机     | 轮趣科技          | 具体数据之后整理                                                          |
| 步进电机   | 串口通讯          | 为云台准备                                                             |
| 继电器    | —             | 驱动大功率电器，备用                                                        |
| JY901P |               | [官方资料](https://wit-motion.yuque.com/wumwnr/docs/ps1fgb429zx1m81z) |
| 42步进电机 | 张大头Emm42_V5.0 | [资料链接](https://pan.baidu.com/s/1HTdlBwxrJ7cd-00j0t3cBQ?pwd=k74a)  |


---

## 二、前置工作

### 1. TI 芯片学习

**目标芯片**：[TImspm0g3507](https://www.ti.com.cn/tool/cn/LP-MSPM0G3507)

| 工具 / 资源                                                   | 说明                 |
| --------------------------------------------------------- | ------------------ |
| CCSTUDIO                                                  | 官方 IDE             |
| MSPM0-SDK                                                 | 软件开发套件，包含驱动、例程、中间件 |
| MSPM0-ACADEMY                                             | 官方教程               |
| [天猛星开发手册](https://wiki.lckfb.com/zh-hans/tmx-mspm0g3507/) | 重点学习               |
| Jlink驱动                                                   |                    |

> [!todo] MSPM0 拓展板
> 已有一版 Gerber 文件，功能待确认。

### 2. 模块驱动库准备

各模块方案已在上方「传感器与驱动」表格中确定，待逐个调试。

> 1. 电机驱动 TB6612（最熟，先跑通）
> 2. 陀螺仪（维特，放弃 MPU6050）
> 3. 红外循迹
> 4. 摄像头 K230
> 5. 步进电机（云台）
> 6. 继电器（备用）



## 三、待做事项
- [ ] 张大头步进电机选取什么样的通讯方式，是否可以使用单串口控制多路电机？
- [ ] jy901p使用i2c还是串口，获取历程实验，编写驱动库




## 四、张大头步进电机
### 单串口控制多电机
![[Pasted image 20260704154614.png]]

### 说明书[[ZDT_X42S第二代闭环步进电机用户手册V1.0.5_260527.pdf]]

## 五、维特陀螺仪
### 1、jy901p
#### 角度输出格式
![[Pasted image 20260706082751.png]]

#### 工作流程
串口中断解析（效率最低，实现最简单）用于测试工程
```c#
/* printf 重定向到 USART1，newlib-nano 要求实现 __io_putchar */
int __io_putchar(int ch)
{
    HAL_UART_Transmit(&huart1, (uint8_t *)&ch, 1, 10);
    return ch;
}
```

`wit_c_sdk.c`协议状态机
每次直接移植，不需要修改
static uint8_t s_ucWitDataBuff[WIT_DATA_BUFF_SIZE];理解为缓冲区，所有的数据被存储在这里面
`void WitSerialDataIn(uint8_t ucData);`状态机函数，当接收中断触发时调用,寻找0x55帧头并不断拼接数据包，直到满足11位数据,经过校验和，拼接字符串第八位和高八位成一个完整的两字节,标志位置1，打印数据



#### DMA + 环形缓冲区 + IDLE 中断

1. 环形缓冲区`ringbuf.h`(0依赖，可移植到任意的项目中！)
`ringbuf_t`结构体
`ringbuf_init`结构体初始化
`ringbuf_put`在中断中写入数据
`ringbuf_get`在主循环中轮询读取数据，返回实际读取的字节数
`uint32_t ringbuf_available(ringbuf_t *rb);`
`uint32_t ringbuf_free(ringbuf_t *rb);`

2. `JY901_P`陀螺仪驱动API
```c#
//封装使用的串口，以后直接使用j_huart和j_hdma_rx代替,便于移植
void jy901p_init(UART_HandleTypeDef *huart, DMA_HandleTypeDef *hdma_rx);

//在空闲中断中，将dma硬件缓冲区的数据搬运到软件环形缓冲区,并清除IDLE标志位
void jy901p_uart_isr(UART_HandleTypeDef *huart);

//
void jy901p_poll(void);
uint8_t jy901p_angle_ready(void);
void jy901p_read_angle(int32_t *roll_10, int32_t *pitch_10, int32_t *yaw_10);
```
