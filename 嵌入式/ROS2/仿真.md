URDF：描述机器人几何结构，传感器和执行器等信息
XML(可标记拓展语言)

/robot_state_publish
launch文件:


# 速记
`share` 就是一个放**所有运行时不依赖机器架构的文件**的地方——launch 文件、URDF 模型、配置文件都放这，方便 ROS 2 运行时通过包名查找到它们

get_package_share_directory 此包用于获取包内具体资源路径

launch文件:用于一键启动多个节点

`launch_ros.actions.Node()`用于实例化一个类

xacro
collision标签：给机器人添加碰撞属性
传感器部件和执行器部件

为机器人添加质量和惯性